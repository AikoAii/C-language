#include <stdio.h>

int main() {
    //use a comma-separated list:
    int x = 5, y = 6, z = 7;
    printf("%d\n", x + y + z);

    //You can also assign the same value to multiple variables of the same type:
    int a,b,c;
    a=1, b=2, c=3;
    printf("%d", a+b+c);

    return 0;
}