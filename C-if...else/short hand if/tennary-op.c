/*
variable = (condition) ? expressionTrue : expressionFalse;
*/

#include <stdio.h>

int main(){
    int time = 15;
    int bangun = (time > 15) ? printf("Yatta") : printf("Cihuyy");

    return 0;
}