#include <stdio.h>

int main() {
    int omkeGas = 0;
    if (omkeGas = 1) /*conditions */ {
        printf("Hidup jokowi!"); //block of code to be executed if the condition is true
    } else {
        printf("YNTKTS"); //block of code to be executed if the condition is true
    }

    return 0;
}